import React from 'react';

import './index.css';

export default function OpBtns() {
    return (
        <div>
            <button>Start</button>
            <button>Reset</button>
            <button>Strict</button>
        </div>
    )
}